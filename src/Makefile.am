METASOURCES = AUTO
if COND_MPI
    LMPI = lmpi
    MPISCALER = mpiscaler
endif
SUBDIRS = ext library $(LMPI) $(MPISCALER) blast2fa env coma makedb makepro \
	pcluster proview pscores pshuffler segpro simal
